# Copy to .env if you use tools that load it (optional).
# This project reads FRUIT360_DATA_ROOT from the environment; set in the shell or here
# only if your tooling supports it — do NOT put Kaggle secrets in the repo.

# FRUIT360_DATA_ROOT=C:\path\to\folder\with\Training\and\Test
